import { PagePlaceholder } from "@/components/ui/Card";

// Addendum v1.1 — cross-site comparison page.
export default function ComparePage() {
  return (
    <PagePlaceholder title="Compare Sites" step="Step 9 (Addendum v1.1)" />
  );
}
